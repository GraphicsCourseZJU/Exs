纹理作为一张图片，可以来源于互联网，或者本地服务器，
但是不能来源于类似C:\pic\a.jpg这样的本地路径。这是因为javascript没有加载本地路径文件的权限。

解决方法：
1. 搭建一个tomcat，apache，iis等等服务器中的一种，并把图片资源放到服务器中，并通过http://localhost:8080/img/a.jpg这样的方式去访问它。
2. 使用firefox浏览器（windows亲测有效），就可以免去建立服务器的麻烦，直接可以读取纹理图片